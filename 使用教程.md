# Henge 使用教程

Henge 是 SillyTavern 的本地词句修订扩展。它在一条回复输出完成后，按你写好的规则找出特定词语或句式，再让你审阅修改，或自动修改并保存。

它不是 AI 润色器：不会理解上下文含义，也不会调用聊天模型或外部 API。它擅长处理“规则明确、会反复出现”的文字，例如删除口癖、替换程度词、精简固定句式。

## 先理解：一条规则本质上是什么

一条 Henge 规则，本质上是一份**确定性的文字查找与替换说明**。可以把它概括成：

> 在允许处理的正文范围里，用 JavaScript 正则表达式找到文字，再按照替换模板删除、替换或保留捕获到的部分。

它主要包含两项内容：

1. **查找**：描述“什么样的文字算命中”。简单时可以是一个词或英文逗号分隔的词表；复杂时是 JavaScript 正则表达式。
2. **替换为**：描述“命中后变成什么”。留空表示删除；填写普通文字表示固定替换；填写多个候选表示随机选择；填写 `$1`、`$2` 等引用表示放回对应括号捕获的原文。

此外，插件还会保存这条规则是否启用、采用审阅还是自动处理等配置，但日常让 AI 帮忙写规则时，最重要的仍是“查找”和“替换为”。

例如：

```text
查找：不是([^，。！？]+)，而是([^。！？]+)
替换为：$2
```

这不是一句给 AI 的自然语言命令，也不是一段会自行判断文意的程序。它是一种模式匹配：程序找到符合结构的原文，第一对括号保存前半段，第二对括号保存后半段，再用 `$2` 放回后半段。相同输入和相同规则会产生相同的匹配范围；只有配置了多个替换候选或随机宏时，替换结果才可能不同。

插件的实际执行过程是：

1. 等待角色回复完整结束。
2. 按标签提取和内容排除设置，划出允许处理的正文范围。
3. 对每条已启用规则运行匹配，并记录原文中的准确位置。
4. 把命中组织成可审阅的句段；互不重叠的全部处理，重叠删除合并范围，删除／替换等不兼容重叠按优先级或随机结果选择一套执行。
5. 按设置等待你确认，或自动应用并保存。

每条规则都对同一份原文进行检测，插件不会把某条规则的替换结果立刻交给下一条规则继续循环匹配。标签、代码、链接和排除内容也不会为了凑出一个句式而被跨越。

普通词表只是方便填写的快捷写法。例如，不含正则符号的 `极其, 极致` 会在内部变成多个可选分支。只要查找内容出现 `?`、`|`、括号、方括号、反斜杠等正则结构，插件就会把整项按正则表达式理解，不再把英文逗号当作普通词表分隔符。

## 直接让 AI 帮你写规则

AI 最适合帮你完成两件事：把自然语言需求转换成正则，以及准备“应该命中／不应命中”的测试句。AI 给出的规则仍可能范围过宽，所以保存前一定要用插件里的「试一下」验证。

可以把下面这段连同你的需求直接发给 AI：

```text
请为 SillyTavern 的 Henge 词句修订插件编写规则。

Henge 规则的本质是：在允许处理的正文范围内，用 JavaScript 正则表达式查找文字，再用替换模板处理命中结果。

规则格式：
- “查找”填写 JavaScript 正则表达式的表达式部分，默认全局匹配，不必添加 /.../g；只有需要特别指定标志时才写完整正则。
- “替换为”留空表示删除；可以填写固定文字、英文逗号分隔的多个随机候选，或使用 $1、$2、$<名称> 引用捕获组。
- 支持 $&、$0、{{match}} 放回整段命中原文。
- 支持 {{user}}、{{char}}、{{random::候选1::候选2}}。
- 规则按文字结构匹配，不理解语义；不能依靠 AI 在运行时判断上下文。
- 不要生成会匹配空位置的正则。
- 尽量限制匹配范围，避免 .* 跨过过多正文；句式优先用 [^，。！？]+ 之类的明确边界。
- 标签、代码、链接和排除内容是不可跨越的边界。

我的需求是：
【在这里描述想删除或替换的词语／句式，并给出几条真实例句】

请按以下格式回答：
1. 规则用途
2. 查找
3. 替换为
4. 逐段解释正则
5. 至少 3 条应该命中的测试句，以及预期结果
6. 至少 3 条不应该命中的测试句
7. 可能误伤的情况和更保守的备选写法

不要只给规则；请检查括号、方括号、转义和捕获组编号是否一致。
```

向 AI 描述需求时，最好同时提供：要处理的原句、期望结果、相似但不该改的反例、是否允许跨逗号或换行。反例越明确，AI 越容易写出范围合适的规则。

## 一、安装和打开

1. 在 SillyTavern 中打开「扩展」→「安装扩展」。
2. 粘贴安装地址：`https://github.com/Mint0oO/sillytavern-text-revision`
3. 安装完成后刷新页面。
4. 点击输入框左侧的魔棒，选择「词句修订」。也可以输入 `/text-revision` 打开。

扩展管理器中显示的名称是 **Henge**。需要 SillyTavern 1.13.5 或更新版本，以及支持原生 `dialog` 的现代浏览器。

## 二、第一次使用：先做一条简单规则

建议第一次先用“审阅”模式，确认规则效果后再考虑自动应用。

1. 打开「规则」，点击「＋ 新建」。
2. 在「查找」中填写 `极其, 极致`。
3. 在「替换为」中填写 `十分, 很`。
4. 展开「试一下」，输入 `他的态度极其冷淡。`，点击「查看修改结果」。
5. 预览正确后，点击「保存规则」。
6. 回到「设置」，开启「回复完成后执行规则」。

之后，新回复中的“极其”或“极致”会被找出，并随机建议替换成“十分”或“很”。规则列表默认只显示紧凑摘要；点开某一条，就能看到完整查找式和编辑框。摘要中的省略号只影响显示，不会改变实际规则。

## 三、认识四个页面

### 修订

这里按正文顺序显示本轮找到的内容：

- 红色删除线：准备删除的原文。
- 绿色文字：准备加入的文字。
- 黄色文字：发现了问题，但没有可直接应用的替换，需要手动编辑。
- 点击整条内容：选择或取消选择。
- 点击右上角铅笔：编辑这一句或这一段。
- 「应用所选」：只应用当前选中的项目，并保存到聊天正文。
- 「完成审阅」：保留剩余内容，本轮不再提醒。
- 「撤销」：恢复本轮最近一次应用。

### 规则

这里管理“找什么、改成什么”。最右侧勾选框控制单条规则是否启用；点击规则文字可以展开编辑。搜索会同时查找“查找”和“替换为”中的内容。

页面上方的统一处理方式有两种：

- 「先审阅，再应用」：只给出建议，确认后才修改。适合新规则和可能误伤的句式。
- 「回复结束后自动应用」：回复完成后直接修改并保存。适合已经反复验证、风险很低的规则。

### 记录

这里保存最近的检测轮次、问题数量和已处理数量。点「查看」可回看当时的结果和修改日志。记录属于当前聊天，不会作为正文发送给模型。

### 设置

这里控制自动检测、界面外观、悬浮球、正文标签提取和内容排除。外观设置只改变显示，不影响规则效果。

## 四、规则从简单到复杂怎么写

### 1. 删除一个词

| 查找 | 替换为 | 效果 |
| --- | --- | --- |
| `极其` | 留空 | 删除“极其” |

替换框完全留空就表示删除。

### 2. 一条规则处理多个普通词

| 查找 | 替换为 | 效果 |
| --- | --- | --- |
| `极其, 极致` | `十分, 很` | 找到前两者之一，并随机换成后两者之一 |

普通词表可以用英文逗号或换行分隔。中文逗号会被当作正文标点，不会用于分隔词表。

### 3. 可有可无的字：`?`

| 查找 | 替换为 | 效果 |
| --- | --- | --- |
| `死死地?` | 留空 | 同时匹配“死死”和“死死地” |

`?` 表示它前面的一个字符或括号组可以出现 0 次或 1 次。这里它只控制“地”。

### 4. 多种写法任选其一：`|`

| 查找 | 替换为 | 效果 |
| --- | --- | --- |
| `这些|那些` | `这些` | 把“这些”或“那些”统一成“这些” |

`|` 表示“或者”。复杂正则中的多个分支应使用 `|`，不要用英文逗号代替。

### 5. 处理固定句式：括号和 `$1`、`$2`

| 查找 | 替换为 | 效果 |
| --- | --- | --- |
| `不是([^，。！？]+)，而是([^。！？]+)` | `$2` | “不是害怕，而是担心。”变成“担心。” |

这条规则可以拆开理解：

- `不是` 和 `，而是`：必须出现的固定文字。
- `[^，。！？]+`：连续匹配一段不是逗号、句号、问号或感叹号的文字。
- 第一对括号保存前半段，叫第 1 个捕获组；第二对括号保存后半段，叫第 2 个捕获组。
- `$2`：替换时放回第 2 个括号捕获到的文字。

如果替换为 `$1`，保留的是前半段；`$&`、`$0` 或 `{{match}}` 表示放回整段命中原文。

### 6. 使用酒馆宏

替换内容支持 SillyTavern 的宏，例如：

- `{{user}}`：当前用户名称。
- `{{char}}`：当前角色名称。
- `{{random::轻轻::缓缓}}`：随机选择一个候选。

如果一份替换内容本身含英文逗号或换行，勾选「整段替换」，避免它被拆成多个随机候选。

## 五、写复杂规则时，按这个顺序最安全

1. 先把处理方式设为「先审阅，再应用」。
2. 新建规则后，使用「试一下」准备至少三种文字：应该命中的、相似但不该命中的、带标点或换行的边界情况。
3. 保存后，在真实回复里观察几轮；有误伤就先停用，不要急着自动应用。
4. 规则长期稳定后，再考虑切换为自动应用。

正则只按文字结构匹配，不理解句意。范围写得太宽，可能从一个词一直匹配到后面的标点。拿不准时，宁可把范围写窄，并保留人工审阅。

## 六、批量添加规则

展开规则页底部的「批量添加」后，每行填写一条：

```text
死死地?
极其, 极致	十分, 很
不是([^，。！？]+)，而是([^。！？]+)	$2
```

第一行只有查找内容，表示删除。第二、三行的查找与替换之间是一个 Tab；可以直接从电子表格复制两列。整批规则必须全部有效才会保存，完全相同的规则会跳过。

## 七、用 JSON 备份或分享整套规则

规则页顶部提供两个入口：

- **导出 JSON**：下载一个 `henge-rules-日期.json` 文件，里面包含当前全部规则、单条启用状态和统一的自动／审阅处理方式。
- **导入 JSON**：选择别人分享的文件或自己的备份。插件会先检查文件和每条规则，确认有效后再显示导入选择。

导入有两种方式：

- **追加**：保留当前规则，把文件里的新规则加到后面；完全相同的规则会跳过。当前的自动／审阅方式不变。
- **替换全部**：清除当前规则，用文件中的规则完整替换，并采用文件保存的自动／审阅方式。适合恢复备份；操作前建议先导出当前规则留底。

规则集 JSON 只用于规则分享，不是整个插件或聊天的备份。它不会包含聊天正文、检测记录、原文快照、标签提取、内容排除、外观、悬浮球或账号信息。不要手工删除 `type`、`formatVersion` 和 `rules` 字段；修改过的文件仍会在导入时逐条校验。

## 八、只修订正文，跳过思考或状态栏

在「设置」中有两个独立入口：

- 「标签提取」：只检查指定标签内部。例如填写 `content`，就只处理 `<content>...</content>` 中的文字。
- 「内容排除」：跳过一对开始、结束文字之间的内容。例如 `<status>` 到 `</status>`。

排除优先于提取。标签、代码和排除内容也是规则不可跨越的边界；插件不会为了匹配一句话而穿过这些内容。

配置后请重新检测。旧检测结果仍对应旧范围，不能代表新设置的效果。

## 九、常见问题

### 为什么没有自动检测？

确认「设置」中的「回复完成后执行规则」已经开启，并确认对应规则右侧的启用勾选框已选中。自动检测在回复完整结束后运行，不会在模型逐字输出时修改正文。

### 为什么找到了内容，却不能直接应用？

可能是规则只负责提示、没有替换内容，或仅提示规则和其他规则在同一位置重叠。点右上角铅笔手动处理，再完成这一条。

### 为什么规则报错？

常见原因是括号或方括号没有闭合、反斜杠写错，或者表达式会匹配“空位置”。回到规则的「试一下」查看错误提示，并从更短的表达式逐步增加条件。

### 为什么保存后正文看起来没变？

如果出现“未能确认保存成功”，先检查网络并刷新聊天，确认服务器上的实际正文，再重新检测。插件只有在回读核对成功后才会报告保存成功。

### 规则会不会把正文发到外部？

不会。规则检测在当前设备完成；扩展没有外部分析服务、跟踪脚本、远程字体或聊天模型请求。规则和外观保存在扩展设置中，检测记录和原文快照保存在当前聊天元数据中。导出聊天时，这些元数据也会随聊天一起导出。

## 十、推荐给新用户的默认方案

- 前几天只用「先审阅，再应用」。
- 一条规则只解决一种明确问题。
- 每条复杂规则都先用「试一下」。
- 只有确定不会误伤的简单词语规则才使用自动应用。
- 需要跳过思考、状态栏或特殊模块时，先配置标签提取与内容排除。

更完整的兼容性、数据边界和开发说明见 [README](README.md)。
