# Henge 使用教程

Henge 是 SillyTavern 的本地词句修订扩展。它在一条回复输出完成后，按你写好的规则找出特定词语或句式，再让你审阅修改，或自动修改并保存。

它不是 AI 润色器：不会理解上下文含义，也不会调用聊天模型或外部 API。它擅长处理“规则明确、会反复出现”的文字，例如删除口癖、替换程度词、精简固定句式。



## 一、安装和打开

1. 在 SillyTavern 中打开「扩展」→「安装扩展」。
2. 粘贴安装地址：`https://github.com/Mint0oO/sillytavern-text-revision`
3. 安装完成后刷新页面。
4. 点击输入框左侧的魔棒，选择「词句修订」。也可以输入 `/text-revision` 打开；若要检查某一条历史 AI 回复，点击该回复编辑铅笔左侧的方框铅笔图标。

扩展管理器中显示的名称是 **Henge**。需要 SillyTavern 1.13.5 或更新版本，以及支持原生 `dialog` 的现代浏览器。

## 二、第一次使用：先做一条简单规则

建议第一次先用“审阅”模式，确认规则效果后再考虑自动应用。

1. 打开「规则」，点击「＋ 新建」。
2. 在「查找」中填写 `极其, 极致`。
3. 在「替换为」中填写 `十分, 很`。
4. 展开「试一下」，输入 `他的态度极其冷淡。`，点击「查看修改结果」。
5. 预览正确后，点击「保存规则」。
6. 回到「设置 → 运行设置」，选择「人工审查」，开启「自动检测」。

之后，新回复中的“极其”或“极致”会被找出，并随机建议替换成“十分”或“很”。规则列表默认只显示紧凑摘要；点开某一条，就能看到完整查找式和编辑框。摘要中的省略号只影响显示，不会改变实际规则。

## 三、认识四个页面

### 修订

这里按正文顺序显示本轮找到的内容：

- 顶部「x处问题/x字段　楼层号#」说明当前结果属于哪一楼。
- 「重新检测」固定检测本次打开面板时选择的楼层；进入规则或设置再返回不会改成最新楼。
- 红色删除线：准备删除的原文。
- 绿色文字：准备加入的文字。
- 黄色文字：发现了问题，但没有可直接应用的替换，需要手动编辑。
- 点击整条内容：选择或取消选择。
- 点击右上角铅笔：编辑这一句或这一段；点击旁边的三点按钮，可选择「恢复」原句或「删除」整句。取消勾选即可保留当前句。
- 「应用所选」：只应用当前选中的项目，并保存到聊天正文。
- 「完成审阅」：保留剩余内容，本轮不再提醒。
- 「撤销」：恢复本轮最近一次应用。

已应用的句段会在右侧显示较深的渐暗边缘，已保留的句段显示较浅的边缘；状态不再占用正文旁的文字位置。

从魔棒、悬浮球、扩展设置或命令打开时，面板选择当时最新的可检测 AI 回复；从某楼旁的方框铅笔打开时，选择该楼。关闭主面板后这次选择失效，下次普通打开会重新选择当时最新回复。若目标已被删除、正文已编辑或回复版本已切换，旧结果不能继续应用，也不会偷偷改到别的楼层。

手动检测和仅需人工审阅的自动检测，算完就会显示结果，记录在后台保存。未完成的整句编辑会保留在当前浏览器会话；同楼、同版本且正文和规则未变时，关闭或刷新后可恢复。自动应用会把检测记录、修改日志和正文一次保存。应用、撤销或完成审阅若未能确认服务器状态，面板会保留本地修改、暂停后续写入，并提供「重新确认」；请先核对再继续。

### 规则

这里管理“找什么、改成什么”。顶部“生效 2 / 3”表示总共 3 条规则，其中 2 条正在使用；分组开关和每条规则最右侧的勾选框都会影响生效数量，切换后立即更新。点击规则文字会打开独立编辑弹窗。搜索会同时查找“查找”和“替换为”中的内容。需要批量删除时，先点顶部「删除」，勾选一条或多条，再用右下角的「确定删除」完成；已有规则也能在编辑弹窗左下角单独删除。编辑时不再另设取消按钮，点击右上角关闭即可；若有未保存修改，会先询问是否放弃。

工具栏下方显示自动检测状态。处理方式可在「设置 → 运行设置」中切换：

- 「人工审查」：只给出建议，确认后才修改。适合新规则和可能误伤的句式。
- 「自动应用」：开启「自动检测」时，在回复结束后自动应用可直接处理的修改并保存；手动检测仍先展示结果。仅提示项和冲突项仍需审阅。适合已经反复验证的规则。

### 记录

这里按 AI 楼层和内容版本显示记录，例如 `10# 1`、`10# 2`。同楼重复检测只更新该版本的详情，真实修改追加到日志。最近 10 层保留句段详情，第 11–99 层只保留修改日志，第 100 层淘汰最旧整层。旧记录转换后可在这里导出原始备份，再清理旧副本。记录属于当前聊天，不会作为正文发送给模型。

### 设置

设置依次分为「运行设置」「检测范围」「界面外观」。运行设置包括「启用插件」「自动检测」「显示逐楼层检测入口」「处理方式」；总开关关闭后不再检测或应用，重新打开后规则和其他设置仍会保留。「显示逐楼层检测入口」关闭后只隐藏每条 AI 回复旁的方框铅笔，其他入口不受影响。界面外观可设置主题、透明度、修订配色和悬浮球；关闭悬浮球时其外观设置收起，重新开启仍保留原值。

## 四、规则从简单到复杂怎么写

### 1. 删除一个词

| 查找   | 替换为 | 效果     |
| ---- | --- | ------ |
| `极其` | 留空  | 删除“极其” |

替换框完全留空就表示删除。

### 2. 一条规则写多个正则分支

| 查找       | 替换为     | 效果                 |
| -------- | ------- | ------------------ |
| `极其, 极致` | `十分, 很` | 找到前两者之一，并随机换成后两者之一 |

表达式最外层的英文逗号和实际换行都用于分项，把这一条规则分成多个正则分支。因此下面两种写法含义相同：

    极其, 极致

    极其
    极致

每个分支仍然是正则，可以写成 `死死地?, 极其`，也可以把两项分成两行。中文逗号会被当作正文标点，不用于分隔分支。

### 3. 可有可无的字：`?`

| 查找     | 替换为 | 效果             |
| ------ | --- | -------------- |
| `死死地?` | 留空  | 同时匹配“死死”和“死死地” |

`?` 表示它前面的一个字符或括号组可以出现 0 次或 1 次。这里它只控制“地”。

### 4. 多种写法任选其一：`|`

| 查找  | 替换为 | 效果   |
| --- | --- | ---- |
| `这些 | 那些` | `这些` |

`|` 表示正则内部的“或者”。顶层英文逗号、换行和 `|` 都可以表达“任选其一”，但逗号或换行会把内容拆成几个独立正则分支，因此各分支中的 `$1`、`$2` 都从头编号。括号内部的逗号不会被拆开。

### 5. 处理固定句式：括号和 `$1`、`$2`

| 查找                         | 替换为  | 效果                  |
| -------------------------- | ---- | ------------------- |
| `不是([^，。！？]+)，而是([^。！？]+)` | `$2` | “不是害怕，而是担心。”变成“担心。” |

这条规则可以拆开理解：

- `不是` 和 `，而是`：必须出现的固定文字。
- `[^，。！？]+`：连续匹配一段不是逗号、句号、问号或感叹号的文字。
- 第一对括号保存前半段，叫第 1 个捕获组；第二对括号保存后半段，叫第 2 个捕获组。
- `$2`：替换时放回第 2 个括号捕获到的文字。

如果替换为 `$1`，保留的是前半段；`$&`、`$0` 或 `{{match}}` 表示放回整段命中原文。

### 6. 使用酒馆宏

替换内容支持 SillyTavern 的宏，例如：

- `{{user}}`：当前用户名称。
- `{{char}}`：当前角色名称。
- `{{random::轻轻::缓缓}}`：随机选择一个候选。

如果一个替换候选本身含英文逗号，请用英文双引号把整个候选包住，避免它被拆成多个随机候选。


先理解：一条规则本质上是什么

规则，本质上是一份**确定性的文字查找与替换说明**。可以把它概括成：

> 在允许处理的正文范围里，用 JavaScript 正则表达式找到文字，再按照替换模板删除、替换或保留捕获到的部分。

它主要包含两项内容：

1. **查找**：描述“什么样的文字算命中”。整条规则始终基于 JavaScript 正则；顶层英文逗号或实际换行只是把它可视化地分成多个独立正则分支。
2. **替换为**：描述“命中后变成什么”。留空表示删除；填写普通文字表示固定替换；填写多个候选表示随机选择；填写 `$1`、`$2` 等引用表示放回对应括号捕获的原文。

此外，插件还会保存这条规则是否启用、所属分组以及高／中／低优先级。分组开关可以一次暂停整组，重新开启后仍保留每条规则的独立开关。优先级只在规则命中范围冲突时裁决，默认是“中”。日常让 AI 帮忙写规则时，最重要的仍是“查找”和“替换为”。

例如：
    查找：不是([^，。！？]+)，而是([^。！？]+)
    替换为：$2

这不是一句给 AI 的自然语言命令，也不是一段会自行判断文意的程序。它是一种模式匹配：程序找到符合结构的原文，第一对括号保存前半段，第二对括号保存后半段，再用 `$2` 放回后半段。相同输入和相同规则会产生相同的匹配范围；只有配置了多个替换候选或随机宏时，替换结果才可能不同。

插件的实际执行过程是：

1. 等待角色回复完整结束。
2. 按标签提取和内容排除设置，划出允许处理的正文范围。
3. 对每条已启用规则运行匹配，并记录原文中的准确位置。
4. 把命中组织成可审阅的句段；互不重叠的全部处理，重叠删除合并范围，删除／替换等不兼容重叠按高／中／低优先级裁决，同级结果在本轮固定。
5. 按设置等待你确认，或自动应用并保存。

每条规则都对同一份原文进行检测，插件不会把某条规则的替换结果立刻交给下一条规则继续循环匹配。标签、代码、链接和排除内容也不会为了凑出一个句式而被跨越。

这里没有在正则之外另建一套“普通词规则”。例如 `极其, 极致` 和分成两行的写法，都会成为同一条规则里的两个正则分支；`死死地?, 极其` 也一样，每个分支都保留自己的正则语法和捕获组编号。

为了不破坏正则本身，括号、字符范围和数量范围内部的逗号和换行会保留，例如 `(甲,乙)`、`[a,b]`、`a{1,3}`。要匹配最外层的英文逗号可写 `\,`。完整的 `/表达式/标志` 自身也按原式运行，多条完整表达式可以分行填写，例如 `/极其/g` 和 `/极致/g`，各自保留标志；也可以与不带斜杠的分支混合，例如 `/aaa/gi, 极其`。要匹配正文里的换行，推荐明确写 `\n`。

规则列表收起时，会把 `.*`、`.+`、`[^。]+`、`[\s\S]*` 这类表示任意内容的部分简写成 `*`，方便快速辨认。这个简写只用于列表显示，点开编辑、导出和真正执行时仍是原来的完整正则。

## 五、写复杂规则时

1. 先把处理方式设为「人工审查」。
2. 新建规则后，使用「试一下」准备至少三种文字：应该命中的、相似但不该命中的、带标点或换行的边界情况。
3. 保存后，在真实回复里观察几轮；有误伤就先停用，不要急着自动应用。
4. 规则长期稳定后，再考虑切换为自动应用。

正则只按文字结构匹配，不理解句意。范围写得太宽，可能从一个词一直匹配到后面的标点。拿不准时，宁可把范围写窄，并保留人工审阅。

## 六、用 JSON 备份或分享整套规则

规则页顶部提供两个入口：

- **导出 JSON**：下载一个 `henge-rules-日期.json` 文件，里面包含当前全部规则、分组、三级优先级、分组及单条规则的开关状态，以及统一的自动／审阅处理方式。
- **导入 JSON**：选择别人分享的文件或自己的备份。插件会先检查文件和每条规则，确认有效后再显示导入选择。

导入有两种方式：

- **追加**：保留当前规则，把文件里的新规则加到后面；同名分组会合并，并保留现有分组开关，完全相同的规则会跳过。当前的自动／审阅方式不变。
- **替换全部**：用文件中的规则和分组完整替换，并采用文件保存的自动／审阅方式。适合恢复备份；操作前建议先导出当前规则留底。

规则集 JSON 只用于规则分享，不是整个插件或聊天的备份。它不会包含聊天正文、检测记录、标签提取、内容排除、外观、悬浮球或账号信息。新版导出格式为 3；旧格式中纯正则规则可以导入，旧字词或占位模板会使整次导入停止。旧版默认数字优先级 0 转成“中”，非零数字转成“高”；若几个不同数字合并为同级，导入界面会提示检查。不要手工删除 `type`、`formatVersion`、`groups` 和 `rules` 字段。

## 八、只修订正文，跳过思考或状态栏

在「设置」中有两个独立入口：

- 「标签提取」：只检查指定标签内部。例如填写 `content`，就只处理 `<content>...</content>` 中的文字。
- 「内容排除」：跳过一对开始、结束文字之间的内容。例如 `<status>` 到 `</status>`。

排除优先于提取。标签、代码和排除内容也是规则不可跨越的边界；插件不会为了匹配一句话而穿过这些内容。

配置后请重新检测。旧检测结果仍对应旧范围，不能代表新设置的效果。

## 九、常见问题

### 为什么没有自动检测？

确认「设置」中的「自动检测」已经开启，并确认对应规则右侧的启用勾选框已选中。自动检测在回复完整结束后运行，不会在模型逐字输出时修改正文。

### 为什么找到了内容，却不能直接应用？

可能是规则只负责提示、没有替换内容，或仅提示规则和其他规则在同一位置重叠。点右上角铅笔手动处理，再完成这一条。

### 为什么规则报错？

常见原因是括号或方括号没有闭合、反斜杠写错，或者表达式会匹配“空位置”。回到规则的「试一下」查看错误提示，并从更短的表达式逐步增加条件。

### 为什么保存后正文看起来没变？

如果出现“未能确认保存成功”，先检查网络并刷新聊天，确认服务器上的实际正文，再重新检测。插件只有在回读核对成功后才会报告保存成功。

### 规则会不会把正文发到外部？

不会。规则检测在当前设备完成；扩展没有外部分析服务、跟踪脚本、远程字体或聊天模型请求。规则和外观保存在扩展设置中，检测记录保存在当前聊天元数据中。导出聊天时，这些元数据也会随聊天一起导出。

## 十、推荐给新用户的默认方案

- 前几天只用「人工审查」。
- 一条规则只解决一种明确问题。
- 每条复杂规则都先用「试一下」。
- 只有确定不会误伤的简单词语规则才使用自动应用。
- 需要跳过思考、状态栏或特殊模块时，先配置标签提取与内容排除。

更完整的兼容性、数据边界和开发说明见 [README](README.md)。
