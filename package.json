{
  "name": "sillytavern-text-revision",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "node --test tests/*.test.js",
    "check": "node --check index.js && node --check engine.js && node --check controller.js && node --check ui.js",
    "dev": "node scripts/dev-server.mjs"
  }
}
