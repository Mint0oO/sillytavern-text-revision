{
  "name": "sillytavern-text-revision",
  "version": "0.6.0",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "node --test tests/*.test.js",
    "check": "node --check index.js && node --check auto-detection.js && node --check engine.js && node --check controller.js && node --check ui.js && node --check language.js",
    "dev": "node scripts/dev-server.mjs"
  }
}
