# 更新日志

## 0.5.0 · 2026-09-04

- 扩展管理中的名称改为 Henge，面板与魔棒入口仍为「词句修订」。
- 背景透明度移至夜间按钮右侧，使用细轨道短滑条，隔离宿主的粗滑条样式。
- 标签提取、内容排除同排等宽显示，手机上也不纵向堆叠。
- 悬浮球缩小为 36 像素，新增独立透明度、五种颜色与跟随美化选项，并提供实时预览。
- 规则启用勾选移到右侧；点击条目原地展开／收起编辑，暂存未保存输入，支持保存与取消。
- 替换词改为英文逗号分隔的紧凑输入，兼容换行、中文标点和带引号的英文逗号。
- 删除默认及旧配置中的「像{A}的孤狼一样」句式；说明改用「不是害怕，而是担心 → 担心」。
- 验证：38 项自动测试通过；模拟酒馆中验证手机布局、宿主滑条样式隔离、图标配色与透明度、规则展开／收起／取消／保存、逗号候选及刷新持久化。

## 0.4.0 · 2026-09-04

- 记录轮次简化为纯数字；问题统计统一为「x处问题/x字段」，字段按可独立修订的句段计数。
- 侧栏宽度增加至约 80%，最窄 300 像素、最宽 1040 像素，不超出屏幕；导航与标题均为 16 像素，增加点击区域。
- 点击整条选择或取消，以浅色背景和左侧细线提示选中；移除逐条勾选框。铅笔独立位于右上角，后续文字使用整行宽度。
- 悬浮球改为可拖动圆形，记忆当前设备浏览器中的位置，适应屏幕变化，区分点击与拖动。
- 默认排除仅保留 think、thinking；精简未修改的旧默认列表，保留自定义排除。标签提取留空或关闭时检测排除后剩余文字。
- 新增暖纸、青雾、淡紫三套柔和界面美化，各自支持日间和夜间；保留极简与独立修订配色。
- 面板图标自带矢量图形，避免依赖宿主图标字体加载。
- 验证：36 项自动测试通过；本地模拟酒馆中通过整条点击、键盘选择、手动编辑、外观切换、默认排除、轮次显示、悬浮球拖动与刷新记忆，以及 320 / 390 / 768 / 1440 像素布局检查。实际手机酒馆兼容性仍需安装后确认。

## 0.3.0 · 2026-09-04

### 修复

- 修复重 roll 后一直显示「正在生成」、单句勾选和应用被锁住的问题。移除持续生成状态锁，收到完成通知后合并检测；应用前仍核对正文、聊天和回复版本，避免覆盖变化后的内容。
- 隔离酒馆主题绘制的勾选标记，消除双勾、底膜和偏移。
- 合并重复完成通知，切换聊天或开始新回复时取消旧的待检测内容；输出未变化时不重复记录。

### 调整

- 默认从左下角魔棒菜单的「词句修订」打开；设置中可开启或关闭悬浮球。
- 检测到问题后自动打开右侧栏。宽度约为屏幕的 68%，最窄 260 像素、最宽 920 像素，兼顾手机输入和长文阅读。
- 主界面顶部收为一行，去掉回复编号、版本和生成状态；检测轮次只在记录里查看。
- 缩小句间留白，保留分隔线；小铅笔在每条右上方，勾选框位于其下方，整句编辑时使用整行宽度。
- 「标签提取」「内容排除」改为两个独立入口，各自展开编辑，支持开关、取消与保存。
- 内容排除支持「开始文字 → 结束文字」成对配置，包括标签、注释片段与 `image###` 等自定义分隔文字；保留旧版排除标签设置。
- 提取标签支持逗号、中文逗号或换行分隔，以及 `Time-Space Module` 这样的带空格自定义标签名。

### 验证

- 34 项自动测试通过，覆盖重 roll、事件去重、单条修订保存、设置迁移、成对排除和正文保护。
- 在模拟酒馆中验证手机侧栏、主题勾选框、魔棒入口、自动弹出、整句编辑与保存后的请求上下文；实际手机及其他扩展的兼容性仍需安装后确认。

## 0.2.0 · 2026-09-04

### 新增

- 设置中的「提取标签」与「排除标签」，支持多标签、多正文块、同名嵌套、中文标签名以及带属性的标签。
- 提取标签未找到时提示并跳过；配置标签配对异常时停止检测。
- 排除内容与标签成为不可编辑边界，规则替换及手动编辑均只处理允许范围内的文字，原始标签、属性、换行和其他内容保持原样。
- 保存检测范围后，旧结果需要重新检测再应用，避免继续使用旧范围。
- README 功能介绍、标签配置示例、升级说明和本更新日志。

### 调整

- 插件显示名称由「正文修订」改为「词句修订」。仓库名、安装 URL、设置存储标识保持不变，原有规则与外观设置继续使用。
- 含标签的回复按边界展示「句段」，防止整句编辑误改保护内容。
- 0.1.0 检测记录保留为只读，需要重新检测后应用。

### 验证

- 增加提取范围、排除优先级、嵌套标签、属性、未闭合标签、代码样例、手改边界和设置变更的测试。
- 通过本地自动测试与模拟酒馆界面检查；仍需在用户的实际酒馆环境确认主题和其他扩展兼容性。

## 0.1.0 · 2026-09-04

- 首次发布，显示名称为「正文修订」。
- 固定词语和有限句式模板检测、多候选随机替换、独立删除按钮、整句编辑与勾选应用。
- 修改写入保存的聊天正文及当前翻页版本，保存后读取聊天核对；支持撤销最近一次应用。
- 聊天、回复版本或正文变化时阻止过期结果覆盖。
- 按轮次保存检测记录。
- 手机独立面板、顶部导航、底部应用栏；支持日夜模式、三组配色和背景透明度。
- 通过 13 项自动测试及 320 / 390 像素宽度的模拟酒馆检查。
